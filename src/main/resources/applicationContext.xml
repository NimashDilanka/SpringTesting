<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">


    <!-- Define your beans here -->
    <bean id="myFortuneService" class="com.lov2code.springdemo.HappyFortuneService"/>

    <bean id="myCoach" class="com.lov2code.springdemo.FootBallCoach">
        <!-- setup constructor injection-->
        <constructor-arg ref="myFortuneService"/>
    </bean>

    <bean id="myCricketCoach" class="com.lov2code.springdemo.CricketCoach">

        <!-- setup setter injection -->
        <property name="fortuneService" ref="myFortuneService"/>

        <!-- setup inject literal values-->
        <property name="emailAddress" value="nimash@gmail.com"/>
        <property name="team" value="Srilankan kill"/>

    </bean>

    <!-- read the property file-->
    <context:property-placeholder location="classpath:sport.properties"/>

    <bean id="myCricketCoach2" class="com.lov2code.springdemo.CricketCoach">

        <!-- setup setter injection -->
        <property name="fortuneService" ref="myFortuneService"/>

        <!-- setup inject literal values-->
        <property name="emailAddress" value="${foo.email}"/>
        <property name="team" value="${foo.team}"/>

    </bean>


    <!-- define ab method to run when ab bean creates and run when bean get destroy-->
    <bean id="myinitCoach"
          class = "com.lov2code.springdemo.TrackCoach"
          init-method="doStartupStuffFunction"
          destroy-method="doEndupStuffFunction">

    </bean>



</beans>